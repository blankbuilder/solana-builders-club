export interface Member {
  name: string
  image: string
}

export interface SiteConfig {
  name: string
  title: string
  description: string
  metaDescription: string
  social: {
    x: string
  }
}
